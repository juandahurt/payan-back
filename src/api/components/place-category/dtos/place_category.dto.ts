export class PYPlaceCategoryDTO {
    title: string;
    numberOfPlaces: number;

    constructor(title: string, numberOfPlaces: number) {
        this.title = title;
        this.numberOfPlaces = numberOfPlaces;
    }
}